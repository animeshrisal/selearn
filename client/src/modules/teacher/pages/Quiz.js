import { ThemeProvider } from "@emotion/react";
import { Grid } from "@mui/material";
import React from "react";



const Quiz = () => {
    return (
        <ThemeProvider>
            <Grid container >
            </Grid>
        </ThemeProvider>
    );
};
export default Quiz;
